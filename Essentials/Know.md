# Know

Sometimes, we hope to claim some knowledge is correct by default. We could use `know` to claim it. But keep in mind, `know` is powerful and dangerous. We would introduce how `know` be used on the following chapters.

> Note: Sometimes, you could set some fact as `know` temporarily and try to prove it later. Then, you can continue your workflow more smoothly.